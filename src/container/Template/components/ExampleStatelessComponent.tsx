import React from "react"
import {Text, View} from "native-base"

interface Props {

}

const s = {
    container: {

    }
}

const MenuList = ({}: Props) => (
    <View style={s.container}>
        <Text>This is an example components</Text>
    </View>)

export default MenuList
